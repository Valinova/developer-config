name: browser-walker
description: Opus exploration subagent that walks a caller-supplied dev URL and flow with minimal browser snapshots.
source: internal (Valinova/developer-config)
version: 2026-04-17
agents:
  - browser-walker
recommended_for:
  - repos running a local dev server that benefits from automated browser walks
requires:
  - playwright MCP server configured
