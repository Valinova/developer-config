# Claude-specific guidance

This file IS `~/.claude/CLAUDE.md` (via symlink — see SETUP.md). It holds
nothing itself: every rule lives in one canonical file under `instructions/`,
imported below. Edit those files, not this one. `model-selection.md` and
`codex-delegation.md` are deliberately not imported: `dispatch-bootstrap.md`
says when to read them.

@~/Development/developer-config/instructions/principles.md
@~/Development/developer-config/instructions/git-operations.md
@~/Development/developer-config/instructions/claude-conventions.md
@~/Development/developer-config/instructions/dispatch-bootstrap.md
